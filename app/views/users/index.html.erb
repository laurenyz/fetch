<h1>All Users:</h1>
<ul>
    <% @users.each do |user| %>
    <li><%= link_to user.cap_user_name, user_path(user) %></li>
    <% end %>
</ul>

<div>
    <%= link_to "Add a User", new_user_path %>
</div>